{
  "name": "camerasoup",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "studio",
    "video",
    "worker"
  ],
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "cert": "bash scripts/cert.sh",
    "start": "npm run build -w studio && node server/src/index.js",
    "server": "node server/src/index.js",
    "build": "npm run build -w studio",
    "deploy": "npm run build -w studio && npm run deploy -w worker",
    "test": "node scripts/integration-test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
